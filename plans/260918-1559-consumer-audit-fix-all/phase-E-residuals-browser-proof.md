# WS-E · residuals from A/B/C + real-browser proof

Runs after ws/a, ws/b, ws/c, ws/d1 are merged into fix/consumer-audit-batch. Read reports/ws-a-report.md, ws-b-report.md, ws-c-report.md first (sections "not fixed", "residual", "caveat").

Owned files: packages/ui/src/components/{input,search-input,textarea,box,field,field-context,date-picker-shared,date-time-picker-shared,toast,safe-area,theme-scope,use-bee-token,select,dialog}.ts(x), packages/core/src/**, apps/visual-regression/** (Playwright specs + fixture app src), examples/web-consumer/** (repro only, revert scaffolding you add), apps/showcase/__tests__/**. Do NOT edit apps/docs (another workstream is editing it); put any docs text you want published into your report under "Docs text to publish".

Items:
1. #606 root cause. apps/visual-regression depends on react-native-web: open its installed `dist/exports/TextInput/index.js` and check whether RNW's own keydown handler calls stopPropagation (upstream RNW has done this for years, comment "Prevent key events bubbling"). Write a Playwright spec that focuses a BeeUI Input, presses a key, and asserts a bubble-phase `document.addEventListener('keydown', …)` listener fires (this is the consumer's assertion). If RNW is the cause: implement a BeeUI-side fix in Input/SearchInput/Textarea web ONLY if it cannot double-fire capture listeners (for example forwarding the event to a BeeUI-owned bubble re-dispatch is NOT acceptable if capture listeners see it twice); otherwise leave the spec as `test.fixme` with the root cause in its title, and write the consumer guidance (capture-phase listeners, code sample) for the Input docs page in the report.
2. #601 residual. `Field.requiredAccessibilityLabel` still defaults to the English literal 'required' and input.tsx / date-picker-shared / date-time-picker-shared append it. Change the default to undefined so no English copy is injected; keep `accessibilityRequired`/`aria-required`; update existing tests that assert "…, required" to assert the new contract; keep `requiredLabel` (added by WS-A) as the localized opt-in.
3. #563 residual. box.tsx forwards raw `className` to the host; route through `cn()` like the rest and add it to the class-name-merge-safety test.
4. #586 Toast placement public prop. WS-C added a `placement` default on the native viewport but no public path. Expose `placement?: 'top' | 'bottom'` on the toast provider/viewport props that BeeUIProvider (safe-area.tsx or wherever the provider lives) forwards; default stays bottom on native, top on web unless the issue says otherwise. Test.
5. Browser proof (Playwright in apps/visual-regression, add fixture screens as needed): (a) #612 a Select with 20 options is pickable by mouse click on the last option; (b) #607 item 1 + #587: an open Dialog has exactly one `[role=dialog]` node, an open AlertDialog exactly one `[role=alertdialog]` and zero `[role=dialog]`; (c) #548 Sheet backdrop covers the viewport when app root is shorter than the viewport.
6. #550/#552 (BeeThemeScope scoped CSS variables ignored in an external npm consumer). Reproduce with the packed path: `examples/web-consumer/setup.sh` packs the workspace packages; build it, render a `BeeThemeScope` with a different brand/appearance inside the app and read the semantic CSS variable / `useBeeToken` value inside vs outside the scope. If reproduced, fix in theme-scope.tsx / use-bee-token.ts / package build config and prove with the same repro; if not reproduced, record the exact commands and observed values.
7. #609: verify with the same packed consumer whether `Uniwind.setTheme('system')` resumes following the OS after `setTheme('dark')`; record the finding. If it does not, add a tiny documented helper in packages/ui (e.g. `followSystemTheme()` that re-subscribes) only if the fix is under ~30 lines; otherwise record for docs.

Gates: `pnpm lint`, `pnpm --filter @beemvp/beeui-ui typecheck`, `pnpm --filter @beemvp/beeui-showcase typecheck`, `pnpm ui-exports:check` (regenerate if exports change), full `pnpm --filter @beemvp/beeui-showcase test`, `pnpm --filter @beemvp/beeui-visual-regression typecheck`, and run the Playwright specs you added (`pnpm --filter @beemvp/beeui-visual-regression exec playwright test <spec>` after `build:web`; install browsers with `pnpm exec playwright install chromium` if missing).
