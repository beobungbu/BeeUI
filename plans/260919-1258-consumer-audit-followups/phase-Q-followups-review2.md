# WS-Q · review #2 items fixed on fix/consumer-audit-followups

Source: plans/reports/from-astra-260920-1827-deep-review-report.md items 3, 4, 6, 8, 9, 10.

Owned: packages/ui/src/components/{toolbar,table.web,table,select,sheet.native}.tsx, docs/component-reference.content.json (+ regenerated pages via `pnpm docs:portal-pages:generate` / `docs:contract:generate`, never a single generator script), docs/components.md, apps/docs/src/content/docs/components/{toolbar,table,select,date-picker,date-time-picker,icon-button,screen}.md hand-authored parts only, apps/showcase/__tests__/**, apps/visual-regression/tests/**. Do NOT touch input/password-input/safe-area/icon-button source (WS-P owns them on the other branch) or docs/dist-tag-policy.md.

1. ToolbarItem contract (item 3). `onPress`, `disabled`, `className` on `ToolbarItem` are only honoured in overflow mode. Decide and implement one model: ToolbarItem metadata is authoritative in both modes — in visible mode clone the child with `disabled` when the item is disabled and with `onPress` when the item provides one and the child has none (explicit child onPress wins; warn once in dev if both are set and differ), and apply `className` to the item's own wrapper. Update the JSDoc so the contract is exact. Tests must NOT duplicate props on the child: `<ToolbarItem disabled onPress={fn}><IconButton/></ToolbarItem>` visible → disabled, press does nothing; enabled → press calls fn; same item collapsed → overflow entry disabled/press calls fn.
2. Toolbar overflow math (item 4). Replace the 36px constant with the measured overflow trigger width (onLayout on the trigger; fall back to the icon control size token until measured) and include the row gap (`gap-1` = 4px; read it from one shared constant used by both the className and the math) between visible items and before the trigger. Tests at the fit threshold: widths that fit exactly with gaps, one pixel over, and the trigger width measured at 44.
3. Table Web interactive-descendant exclusion (item 6). Extend the selector to the full interactive set: button, a[href], input, select, textarea, summary, [contenteditable], [tabindex]:not([tabindex="-1"]) inside the row (excluding the row itself), and roles button, link, checkbox, radio, switch, menuitem, menuitemcheckbox, menuitemradio, tab, option, combobox, listbox, textbox, searchbox, slider, spinbutton, scrollbar. Playwright (apps/visual-regression, real DOM): row with Button, Checkbox, Radio/SegmentedControlItem, SelectTrigger, Link → activating the child does not fire the row; clicking a plain cell does. Keep the Table page text that discourages `onPress` rows containing interactive controls on native.
4. Docs drift (item 8). DatePicker and DateTimePicker pages: remove the "locale does not translate the placeholder" limitation (both now localize placeholder/Done; DateTimePicker also Done) and state what locale does translate. docs/components.md: Screen (`scroll` prop), IconButton (`size`, `count`), Toolbar family, Tabs closable/scrollable, Table density/align/onPress, SafeArea composition note (WS-P will change SafeArea to compose inset + inner padding; write the sentence accordingly), SearchInput trailing, Chip static/deselect, ListItem active. Then `pnpm docs:portal-pages:generate && pnpm docs:contract:generate` and all docs checks. Add a scripts/__tests__ case if a check can catch the specific stale sentence pattern (e.g. a "Limitations" claim contradicted by a locale table) — optional, only if cheap.
5. Select `scrollViewProps` (item 9). Web renders a plain overflow View; narrow the public type to the props that both hosts honour (or split into `scrollViewProps` native-only + `listProps`), update JSDoc and the Select page, and add a type-level test. Do not reintroduce ScrollView on Web (#612).
6. Sheet multi-dismiss race (item 10). Deterministic jest with the gorhom mock: open→close→open→close→open with two delayed `onDismiss` callbacks arriving out of order; the sheet must end presented, `onOpenChange(false)` emitted only for real user dismissals. If it fails, replace the scalar dismiss generation with per-request identities.

Gates: `pnpm lint`, ui/showcase/visual-regression typecheck, full `pnpm --filter @beemvp/beeui-showcase test`, `pnpm ui-exports:check`, the Playwright specs you add, `pnpm docs:portal-pages:check && pnpm docs:contract:check && pnpm docs:surface:check && pnpm llms:generate && pnpm llms:check && pnpm docs:examples:check`.
