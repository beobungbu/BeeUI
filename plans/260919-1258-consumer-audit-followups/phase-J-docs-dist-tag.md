# WS-J · #585 prose passes + dist-tag policy aligned with real npm state

Owned: apps/docs/**, docs/dist-tag-policy.md, README.md, docs/ai-agent-cookbook.md, scripts/** only for the checks that read dist-tag-policy (release-control-plane, dist-policy, llms, ai-contract generators/tests), llms*.txt via regeneration. Do NOT edit packages/**.

1. #585 items 2 and 3 (read `gh issue view 585 --repo beobungbu/BeeUI` and plans/260918-1559-consumer-audit-fix-all/reports/ws-d2-report.md + ws-h-report.md for what was already done): the "maintainer's seat" prose pass and the missing prerequisite lines. Work page by page; keep each edit minimal and factual; do not restyle pages that the issue did not flag.
2. #561 option (b), decided by the owner on 2026-09-19: real npm state is `next` = `latest` = 0.86.2-rc.1 for @beemvp/beeui-ui and @beemvp/beeui-cli (verify again with `npm view <pkg> dist-tags --json`). Rewrite docs/dist-tag-policy.md so the stated invariant matches reality: npm points a package's first-ever publish at `latest` regardless of `--tag`, so until a stable release is published `latest` resolves to the RC; describe what changes at the first stable publish (`latest` moves to the stable version, `next` keeps tracking prereleases). Update README, cookbook, troubleshooting and any page that still says `latest` is not promoted / a bare `npm install` will fail. Then make every check that reads the policy agree: `pnpm release-control-plane:check && pnpm release-control-plane:test && pnpm dist-policy:check && pnpm dist-policy:test && pnpm docs:public-truth:check && pnpm llms:generate && pnpm llms:check && pnpm ai-contract:check`. Do not weaken a check to pass it; change its expectation to the new documented invariant and say so in the report.

Gates: the commands above plus `pnpm docs:contract:check && pnpm docs:portal-pages:check && pnpm docs:surface:check && pnpm docs:examples:check && pnpm site:contract:check && pnpm docs:build`.
