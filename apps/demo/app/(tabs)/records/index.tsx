import * as React from 'react';
import { RoutePlaceholder } from '../../../src/shell/route-placeholder';

export default function RecordsRoute() {
  return (
    <RoutePlaceholder
      description="The searchable, filterable, sortable records table lands here"
      issue="#260"
      title="Records"
    />
  );
}
