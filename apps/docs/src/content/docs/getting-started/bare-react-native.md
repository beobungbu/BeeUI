---
title: Bare React Native
description: Run BeeUI in a bare React Native project without Expo.
---

BeeUI is verified against a fresh, package-installed bare React Native consumer as part of
its CI, independent of the Expo runtime.

:::note[Content pending]
Full bare React Native setup steps are tracked for a follow-up docs content issue and will
be sourced from the repository's bare-consumer verification scripts once the public
package surface is stable.
:::

See [Compatibility](/compatibility/) for the exact tested React Native/React/Node version
combination.
