---
title: "Product Feed"
description: "Browsable product grid with search and favorites."
---

<!-- Generated by scripts/public-pattern-reference.mjs. Do not hand-edit. -->

Browsable product grid with search and favorites.

## Preview

[Open the exact pattern in the real BeeUI Web Showcase](/showcase/?surface=pattern&id=product-feed&embed=1). This is a Web runtime preview, not native-device evidence.

Each named state is directly addressable:

- [`default`](/showcase/?surface=pattern&id=product-feed&state=default&embed=1)
- [`loading`](/showcase/?surface=pattern&id=product-feed&state=loading&embed=1)

The same source is available at [`apps/showcase/patterns/commerce-social/screens/product-feed-screen.tsx`](https://github.com/beobungbu/BeeUI/blob/main/apps/showcase/patterns/commerce-social/screens/product-feed-screen.tsx).

## Composition

Principal public BeeUI exports used by this screen: [`Badge`](/docs/components/badge/), [`Box`](/docs/components/box/), [`Button`](/docs/components/button/), [`Card`](/docs/components/card/), [`Chip`](/docs/components/chip/), [`ChipGroup`](/docs/components/chip/), [`HStack`](/docs/components/stack/), [`SearchInput`](/docs/components/search-input/), [`Skeleton`](/docs/components/skeleton/), [`Text`](/docs/components/text/), [`VStack`](/docs/components/stack/).

The pattern is a composition recipe rather than a new framework layer. Follow the linked component contracts for state, provider, platform and accessibility details.

## State and callback contract

The screen is controlled through `ProductFeedScreenProps`. User intent crosses the application boundary through `onFavorite`, `onProductSelect`, `onSearch`; fetching, routing, persistence and side effects remain application-owned.

```tsx
export type ProductFeedScreenProps = {
  loading?: boolean;
  onFavorite?: (product: Product) => void;
  onProductSelect?: (product: Product) => void;
  onSearch?: (query: string) => void;
};
```

Common product states such as loading, success, empty, error, permission-like recovery, filtering/search/selection or pagination are represented only where the actual screen source exposes them. The pattern docs do not invent backend states that the fixture does not render.

## Responsive contract

This pattern follows BeeUI's [mobile-first responsive contract](/docs/responsive/): narrow-phone composition is the baseline; larger widths may reflow or promote navigation/layout according to BeeUI breakpoint tokens. Safe-area and scroll ownership stay explicit, and short-height/landscape/large-text constraints must be handled without viewport-level horizontal scrolling.

## Accessibility

The screen inherits the semantics, touch-target, focus/keyboard, RTL, large-text and reduced-motion contracts of its component composition. Use the [Accessibility guide](/docs/accessibility/) for evidence scope. A Web preview does not substitute for VoiceOver/TalkBack runtime evidence.

## Application ownership boundary

**Intentionally excluded:** Catalog fetching, search execution, and favorite persistence.

BeeUI does not take ownership of app routing, authentication/business rules, APIs/data fetching, persistence, form/state frameworks, chart frameworks or backend logic merely because a pattern visually composes those product concepts.

## Source ownership

`ProductFeedScreen` is **Showcase source you copy**, not a package export: it is not shipped from any `@beemvp/beeui-*` package, and the Registry CLI (`pnpm beeui list` / `pnpm beeui add`) does not carry pattern screens. Copy [`apps/showcase/patterns/commerce-social/screens/product-feed-screen.tsx`](https://github.com/beobungbu/BeeUI/blob/main/apps/showcase/patterns/commerce-social/screens/product-feed-screen.tsx) into your app and adapt it directly. Only the individual BeeUI components it composes (linked above) are available for source ownership through the repository-local Registry workflow; before public CLI publication, use [CLI & source ownership](/docs/guides/cli-source-ownership/) from a BeeUI checkout rather than a public `npx` command.

## Related

- [All production patterns](/docs/patterns/)
- [Component reference](/docs/components/)
- [Showcase](/showcase/)
- [Production reference app](/demo/)
- [Source](https://github.com/beobungbu/BeeUI/blob/main/apps/showcase/patterns/commerce-social/screens/product-feed-screen.tsx)
