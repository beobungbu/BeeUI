---
title: "Transaction Detail"
description: "Read-only transaction detail with receipt and help actions."
---

<!-- Generated by scripts/public-pattern-reference.mjs. Do not hand-edit. -->

Read-only transaction detail with receipt and help actions.

## Preview

[Open the exact pattern in the real BeeUI Web Showcase](/showcase/?surface=pattern&id=transaction-detail&embed=1). This is a Web runtime preview, not native-device evidence.

This screen exposes a single default state.

The same source is available at [`apps/showcase/patterns/dashboard-finance/screens/transaction-detail-screen.tsx`](https://github.com/beobungbu/BeeUI/blob/main/apps/showcase/patterns/dashboard-finance/screens/transaction-detail-screen.tsx).

## Composition

Principal public BeeUI exports used by this screen: [`Badge`](/docs/components/badge/), [`Button`](/docs/components/button/), [`Card`](/docs/components/card/), [`DescriptionItem`](/docs/components/description-list/), [`DescriptionList`](/docs/components/description-list/), [`HStack`](/docs/components/stack/), [`Text`](/docs/components/text/), [`VStack`](/docs/components/stack/).

The pattern is a composition recipe rather than a new framework layer. Follow the linked component contracts for state, provider, platform and accessibility details.

## State and callback contract

The screen is controlled through `TransactionDetailScreenProps`. User intent crosses the application boundary through `onGetHelp`, `onReceipt`; fetching, routing, persistence and side effects remain application-owned.

```tsx
export type TransactionDetailScreenProps = {
  data: TransactionDetailData;
  onGetHelp?: () => void;
  onReceipt?: () => void;
};
```

Common product states such as loading, success, empty, error, permission-like recovery, filtering/search/selection or pagination are represented only where the actual screen source exposes them. The pattern docs do not invent backend states that the fixture does not render.

## Responsive contract

Read from the screen file and the 1 pattern-local file it imports: `transaction-detail-screen.tsx`, `screen-shell.tsx`. Every fact below is scoped to those files and to nothing else.

- **BeeUI layout primitives rendered:** `Box` (`screen-shell.tsx`), `HStack` (`transaction-detail-screen.tsx`), `VStack` (`transaction-detail-screen.tsx`).
- **Scroll ownership:** `ScrollView` (`screen-shell.tsx`).
- **Horizontal scrolling:** no `horizontal` scroll container in `transaction-detail-screen.tsx`, `screen-shell.tsx`.
- **Width constraint:** `max-w-6xl` (`screen-shell.tsx`).
- **Breakpoint-prefixed utility classes:** none in `transaction-detail-screen.tsx`, `screen-shell.tsx`.
- **Platform-prefixed utility classes:** none in `transaction-detail-screen.tsx`, `screen-shell.tsx`.
- **Platform branching:** no `Platform.OS` or `Platform.select` call in `transaction-detail-screen.tsx`, `screen-shell.tsx`.
- **Viewport measurement:** no `useWindowDimensions`, `Dimensions.get` or breakpoint hook in `transaction-detail-screen.tsx`, `screen-shell.tsx`.

BeeUI's [mobile-first responsive contract](/docs/responsive/) is the framework-level document; the list above states only what this screen's own files declare, and says nothing about how the composed components behave internally.

## Accessibility

Read from the screen file and the 1 pattern-local file it imports: `transaction-detail-screen.tsx`, `screen-shell.tsx`. Every fact below is scoped to those files and to nothing else.

- **Roles this screen sets itself:** none set in `transaction-detail-screen.tsx`, `screen-shell.tsx`.
- **Accessibility states and properties it sets itself:** none set in `transaction-detail-screen.tsx`, `screen-shell.tsx`.
- **Semantics inherited from composed BeeUI families:** [`AppHeader`](/docs/components/app-header/), [`Badge`](/docs/components/badge/), [`Box`](/docs/components/box/), [`Button`](/docs/components/button/), [`Card`](/docs/components/card/), [`DescriptionItem`](/docs/components/description-list/), [`DescriptionList`](/docs/components/description-list/), [`HStack`](/docs/components/stack/), [`Text`](/docs/components/text/), [`VStack`](/docs/components/stack/) — each family's own page derives the roles and states it sets; they are not restated here.

Touch-target size, focus order, announcements, RTL, large-text and reduced-motion behavior are not derived from this source — see the [Accessibility guide](/docs/accessibility/) for what is and is not covered by evidence. A Web preview does not substitute for VoiceOver/TalkBack runtime evidence.

## Application ownership boundary

**Intentionally excluded:** Transaction retrieval and receipt generation.

BeeUI does not take ownership of app routing, authentication/business rules, APIs/data fetching, persistence, form/state frameworks, chart frameworks or backend logic merely because a pattern visually composes those product concepts.

## Source ownership

`TransactionDetailScreen` is **Showcase source you copy**, not a package export: it is not shipped from any `@beemvp/beeui-*` package, and the Registry CLI (`pnpm beeui list` / `pnpm beeui add`) does not carry pattern screens. Copy [`apps/showcase/patterns/dashboard-finance/screens/transaction-detail-screen.tsx`](https://github.com/beobungbu/BeeUI/blob/main/apps/showcase/patterns/dashboard-finance/screens/transaction-detail-screen.tsx) into your app and adapt it directly. Only the individual BeeUI components it composes (linked above) are available for source ownership through the repository-local Registry workflow; before public CLI publication, use [CLI & source ownership](/docs/guides/cli-source-ownership/) from a BeeUI checkout rather than a public `npx` command.

## Related

- [All production patterns](/docs/patterns/)
- [Component reference](/docs/components/)
- [Showcase](/showcase/)
- [Production reference app](/demo/)
- [Source](https://github.com/beobungbu/BeeUI/blob/main/apps/showcase/patterns/dashboard-finance/screens/transaction-detail-screen.tsx)
