<!-- Generated by scripts/generate-pattern-library.mjs from the apps/showcase/patterns sources and docs/pattern-library.content.json. Do not edit by hand; run `pnpm docs:patterns:generate`. -->

# BeeUI production pattern library

> 37 production screen patterns across 4 domain packs, composed entirely from public `@beemvp/beeui-ui` components. Each entry links to its executable, typechecked source in the [@beemvp/beeui-showcase](../apps/showcase) Pattern Gallery. Derived from the showcase pattern sources and docs/pattern-library.content.json; enforced by `pnpm docs:patterns:check`.

STATUS: BeeUI is pre-1.0 and UNPUBLISHED. The Pattern Gallery is an in-repo reference application, not a published template. Screens are original BeeUI compositions you own via source-ownership (`pnpm beeui add <component>`), not a redistributable third-party template.

Each pattern documents its purpose, state contract, composition, the callback
ownership boundary between the screen and the application, and the application
logic (data fetching, navigation, persistence, provider integration) it
intentionally leaves to the caller. The patterns are UI compositions only — BeeUI
owns no routing, data, or backend. For single components see the [component
reference](component-reference.md); for the enforcement rules see the [component
documentation contract](component-documentation-contract.md).

## Index

- **Authentication + Onboarding** (9): forgot-password, interests-onboarding, password-updated, profile-setup, reset-password, sign-in, sign-up, verify-code, welcome
- **Dashboard + Finance** (8): analytics, dashboard-overview, invoice-detail, payment-methods, subscription, transaction-detail, transactions, wallet
- **Commerce + Social** (12): cart, checkout, messages, notifications, order-detail, orders, post-detail, product-detail, product-feed, product-search, social-feed, user-profile
- **Account + Settings** (8): account, appearance, change-password, edit-profile, notification-settings, privacy-security, profile, settings

## Authentication + Onboarding

9 screens — source under [`apps/showcase/patterns/auth`](../apps/showcase/patterns/auth).

### Forgot Password

- **Purpose:** Request a password-reset code for an account email.
- **State contract:** `ForgotPasswordScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Button`, `Card`, `Field`, `Input`, `Link`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onBackToSignIn`, `onEmailChange`, `onSubmit`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/auth/screens/forgot-password-screen.tsx`](../apps/showcase/patterns/auth/screens/forgot-password-screen.tsx).
- **Excluded application logic:** Sending the reset email, rate limiting, and account lookup.

### Interests Onboarding

- **Purpose:** Let a new user pick interest topics during onboarding.
- **State contract:** `InterestsOnboardingScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Button`, `Card`, `Chip`, `ChipGroup`, `HStack`, `Link`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onContinue`, `onSelectionChange`, `onSkip`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/auth/screens/interests-onboarding-screen.tsx`](../apps/showcase/patterns/auth/screens/interests-onboarding-screen.tsx).
- **Excluded application logic:** Topic catalog fetching and persistence of selected interests.

### Password Updated

- **Purpose:** Confirmation surface after a password change succeeds.
- **State contract:** `PasswordUpdatedScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Box`, `Button`, `Card`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onContinue`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/auth/screens/password-updated-screen.tsx`](../apps/showcase/patterns/auth/screens/password-updated-screen.tsx).
- **Excluded application logic:** The password mutation and any post-change session handling.

### Profile Setup

- **Purpose:** Collect display name, username, bio, and photo for a new profile.
- **State contract:** `ProfileSetupScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `AlertBanner`, `Avatar`, `Button`, `Card`, `Field`, `Input`, `Link`, `Textarea`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onBack`, `onBioChange`, `onChangePhoto`, `onDisplayNameChange`, `onSkip`, `onSubmit`, `onUsernameChange`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/auth/screens/profile-setup-screen.tsx`](../apps/showcase/patterns/auth/screens/profile-setup-screen.tsx).
- **Excluded application logic:** Username availability checks, image upload, and profile persistence.

### Reset Password

- **Purpose:** Enter and confirm a new password behind a reset flow.
- **State contract:** `ResetPasswordScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Button`, `Card`, `Field`, `PasswordInput`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onConfirmPasswordChange`, `onPasswordChange`, `onSubmit`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/auth/screens/reset-password-screen.tsx`](../apps/showcase/patterns/auth/screens/reset-password-screen.tsx).
- **Excluded application logic:** Reset-token validation and the password mutation.

### Sign In

- **Purpose:** Email/password sign-in with social and forgot-password affordances.
- **State contract:** `SignInScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Button`, `Card`, `Field`, `Input`, `Link`, `PasswordInput`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onAppleSignIn`, `onCreateAccount`, `onEmailChange`, `onForgotPassword`, `onGoogleSignIn`, `onPasswordChange`, `onSubmit`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/auth/screens/sign-in-screen.tsx`](../apps/showcase/patterns/auth/screens/sign-in-screen.tsx).
- **Excluded application logic:** Credential verification, OAuth flows, and session creation.

### Sign Up

- **Purpose:** Account creation with terms acceptance and social sign-up.
- **State contract:** `SignUpScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `AlertBanner`, `Button`, `Card`, `Checkbox`, `Field`, `Input`, `Link`, `PasswordInput`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onAcceptedTermsChange`, `onAppleSignIn`, `onConfirmPasswordChange`, `onEmailChange`, `onGoogleSignIn`, `onNameChange`, `onPasswordChange`, `onSignIn`, `onSubmit`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/auth/screens/sign-up-screen.tsx`](../apps/showcase/patterns/auth/screens/sign-up-screen.tsx).
- **Excluded application logic:** Account creation, validation rules, OAuth, and terms storage.

### Verify Code

- **Purpose:** Enter a one-time verification code with resend and destination change.
- **State contract:** `VerifyCodeScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Button`, `Card`, `Field`, `HStack`, `Link`, `OTPInput`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onChangeDestination`, `onCodeChange`, `onComplete`, `onResend`, `onSubmit`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/auth/screens/verify-code-screen.tsx`](../apps/showcase/patterns/auth/screens/verify-code-screen.tsx).
- **Excluded application logic:** Code delivery, verification, and resend throttling.

### Welcome

- **Purpose:** First-run welcome surface routing to get-started or sign-in.
- **State contract:** `WelcomeScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Box`, `Button`, `Card`, `HStack`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onGetStarted`, `onSignIn`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/auth/screens/welcome-screen.tsx`](../apps/showcase/patterns/auth/screens/welcome-screen.tsx).
- **Excluded application logic:** Navigation and any onboarding state.

## Dashboard + Finance

8 screens — source under [`apps/showcase/patterns/dashboard-finance`](../apps/showcase/patterns/dashboard-finance).

### Analytics

- **Purpose:** Range-filtered analytics dashboard with metric summaries.
- **State contract:** `AnalyticsScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Card`, `EmptyState`, `HStack`, `Progress`, `Stat`, `StatLabel`, `StatValue`, `Tabs`, `TabsList`, `TabsTrigger`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onRangeChange`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/dashboard-finance/screens/analytics-screen.tsx`](../apps/showcase/patterns/dashboard-finance/screens/analytics-screen.tsx).
- **Excluded application logic:** Metric aggregation, data fetching, and chart rendering.

### Dashboard Overview

- **Purpose:** Finance dashboard overview with balances, activity, and primary actions.
- **State contract:** `DashboardOverviewScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Button`, `Card`, `EmptyState`, `HStack`, `Skeleton`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onCreateInvoice`, `onPrimaryAction`, `onTransactionPress`, `onViewActivity`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/dashboard-finance/screens/dashboard-overview-screen.tsx`](../apps/showcase/patterns/dashboard-finance/screens/dashboard-overview-screen.tsx).
- **Excluded application logic:** Data fetching, invoice creation, and navigation.

### Invoice Detail

- **Purpose:** Read-only invoice detail with download and share actions.
- **State contract:** `InvoiceDetailScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Badge`, `Button`, `Card`, `DescriptionItem`, `DescriptionList`, `HStack`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onDownload`, `onShare`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/dashboard-finance/screens/invoice-detail-screen.tsx`](../apps/showcase/patterns/dashboard-finance/screens/invoice-detail-screen.tsx).
- **Excluded application logic:** Invoice retrieval, PDF generation, and share transport.

### Payment Methods

- **Purpose:** List and manage saved payment methods.
- **State contract:** `PaymentMethodsScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Badge`, `Button`, `Card`, `HStack`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onAddMethod`, `onManageMethod`, `onRemoveMethod`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/dashboard-finance/screens/payment-methods-screen.tsx`](../apps/showcase/patterns/dashboard-finance/screens/payment-methods-screen.tsx).
- **Excluded application logic:** Payment-provider integration and method mutations.

### Subscription

- **Purpose:** Current subscription plan with upgrade, manage, and cancel actions.
- **State contract:** `SubscriptionScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Badge`, `Button`, `Card`, `HStack`, `Progress`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onCancel`, `onManage`, `onUpgrade`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/dashboard-finance/screens/subscription-screen.tsx`](../apps/showcase/patterns/dashboard-finance/screens/subscription-screen.tsx).
- **Excluded application logic:** Billing-provider integration and plan mutations.

### Transaction Detail

- **Purpose:** Read-only transaction detail with receipt and help actions.
- **State contract:** `TransactionDetailScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Badge`, `Button`, `Card`, `DescriptionItem`, `DescriptionList`, `HStack`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onGetHelp`, `onReceipt`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/dashboard-finance/screens/transaction-detail-screen.tsx`](../apps/showcase/patterns/dashboard-finance/screens/transaction-detail-screen.tsx).
- **Excluded application logic:** Transaction retrieval and receipt generation.

### Transactions

- **Purpose:** Searchable, filterable transaction list with loading/error/retry states.
- **State contract:** `TransactionsScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Button`, `Card`, `EmptyState`, `ErrorState`, `SearchInput`, `Tabs`, `TabsList`, `TabsTrigger`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onFilterChange`, `onQueryChange`, `onRetry`, `onSearch`, `onTransactionPress`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/dashboard-finance/screens/transactions-screen.tsx`](../apps/showcase/patterns/dashboard-finance/screens/transactions-screen.tsx).
- **Excluded application logic:** Query execution, pagination, and data fetching.

### Wallet

- **Purpose:** Wallet home with accounts, balances, and money-movement actions.
- **State contract:** `WalletScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Button`, `Card`, `EmptyState`, `HStack`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onAccountPress`, `onAddMoney`, `onAddPaymentMethod`, `onReceive`, `onSend`, `onTransactionPress`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/dashboard-finance/screens/wallet-screen.tsx`](../apps/showcase/patterns/dashboard-finance/screens/wallet-screen.tsx).
- **Excluded application logic:** Balance retrieval and send/receive/transfer execution.

## Commerce + Social

12 screens — source under [`apps/showcase/patterns/commerce-social`](../apps/showcase/patterns/commerce-social).

### Cart

- **Purpose:** Shopping cart with quantity edits, removal, and checkout.
- **State contract:** `CartScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Button`, `Card`, `EmptyState`, `HStack`, `Separator`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onCheckout`, `onQuantityChange`, `onRemove`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/commerce-social/screens/cart-screen.tsx`](../apps/showcase/patterns/commerce-social/screens/cart-screen.tsx).
- **Excluded application logic:** Cart persistence, pricing/tax calculation, and checkout routing.

### Checkout

- **Purpose:** Order review with address editing and place-order action.
- **State contract:** `CheckoutScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `AlertBanner`, `Button`, `Checkbox`, `HStack`, `Radio`, `RadioGroup`, `Separator`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onEditAddress`, `onPlaceOrder`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/commerce-social/screens/checkout-screen.tsx`](../apps/showcase/patterns/commerce-social/screens/checkout-screen.tsx).
- **Excluded application logic:** Address validation, payment capture, and order creation.

### Messages

- **Purpose:** Conversation list for a direct-messages inbox.
- **State contract:** `MessagesScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `EmptyState`, `SearchInput`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onConversationSelect`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/commerce-social/screens/messages-screen.tsx`](../apps/showcase/patterns/commerce-social/screens/messages-screen.tsx).
- **Excluded application logic:** Message fetching, realtime updates, and thread navigation.

### Notifications

- **Purpose:** Notification feed with mark-read and selection.
- **State contract:** `NotificationsScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Avatar`, `Badge`, `Button`, `Card`, `EmptyState`, `HStack`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onMarkRead`, `onSelect`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/commerce-social/screens/notifications-screen.tsx`](../apps/showcase/patterns/commerce-social/screens/notifications-screen.tsx).
- **Excluded application logic:** Notification fetching and read-state persistence.

### Order Detail

- **Purpose:** Order detail with reorder and support actions.
- **State contract:** `OrderDetailScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Badge`, `Button`, `Card`, `HStack`, `Separator`, `Text`, `Timeline`, `TimelineItem`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onReorder`, `onSupport`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/commerce-social/screens/order-detail-screen.tsx`](../apps/showcase/patterns/commerce-social/screens/order-detail-screen.tsx).
- **Excluded application logic:** Order retrieval, reorder logic, and support routing.

### Orders

- **Purpose:** Order history list with selection.
- **State contract:** `OrdersScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Badge`, `Button`, `Card`, `EmptyState`, `HStack`, `SegmentedControl`, `SegmentedControlItem`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onOrderSelect`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/commerce-social/screens/orders-screen.tsx`](../apps/showcase/patterns/commerce-social/screens/orders-screen.tsx).
- **Excluded application logic:** Order fetching and pagination.

### Post Detail

- **Purpose:** Social post detail with like, share, and comment submission.
- **State contract:** `PostDetailScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Avatar`, `Button`, `Card`, `HStack`, `Input`, `Separator`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onCommentSubmit`, `onLike`, `onShare`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/commerce-social/screens/post-detail-screen.tsx`](../apps/showcase/patterns/commerce-social/screens/post-detail-screen.tsx).
- **Excluded application logic:** Post/comment fetching and mutation persistence.

### Product Detail

- **Purpose:** Product detail with add-to-cart and favorite actions.
- **State contract:** `ProductDetailScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Badge`, `Button`, `Card`, `Chip`, `ChipGroup`, `HStack`, `Separator`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onAddToCart`, `onFavorite`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/commerce-social/screens/product-detail-screen.tsx`](../apps/showcase/patterns/commerce-social/screens/product-detail-screen.tsx).
- **Excluded application logic:** Product fetching, inventory, and cart mutations.

### Product Feed

- **Purpose:** Browsable product grid with search and favorites.
- **State contract:** `ProductFeedScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Badge`, `Box`, `Button`, `Card`, `Chip`, `ChipGroup`, `HStack`, `SearchInput`, `Skeleton`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onFavorite`, `onProductSelect`, `onSearch`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/commerce-social/screens/product-feed-screen.tsx`](../apps/showcase/patterns/commerce-social/screens/product-feed-screen.tsx).
- **Excluded application logic:** Catalog fetching, search execution, and favorite persistence.

### Product Search

- **Purpose:** Product search results with sort and filter affordances.
- **State contract:** `ProductSearchScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Box`, `Button`, `Card`, `Chip`, `ChipGroup`, `EmptyState`, `HStack`, `SearchInput`, `Skeleton`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onFavorite`, `onFilterPress`, `onProductSelect`, `onSortPress`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/commerce-social/screens/product-search-screen.tsx`](../apps/showcase/patterns/commerce-social/screens/product-search-screen.tsx).
- **Excluded application logic:** Search execution, sorting/filtering logic, and data fetching.

### Social Feed

- **Purpose:** Social timeline with like, comment, share, and compose.
- **State contract:** `SocialFeedScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Avatar`, `Button`, `Card`, `EmptyState`, `HStack`, `Skeleton`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onComment`, `onCompose`, `onLike`, `onPostOpen`, `onShare`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/commerce-social/screens/social-feed-screen.tsx`](../apps/showcase/patterns/commerce-social/screens/social-feed-screen.tsx).
- **Excluded application logic:** Feed fetching, realtime updates, and post persistence.

### User Profile

- **Purpose:** Public user profile with follow and post-open actions.
- **State contract:** `UserProfileScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Avatar`, `Button`, `Card`, `EmptyState`, `HStack`, `Separator`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onEdit`, `onFollow`, `onPostOpen`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/commerce-social/screens/user-profile-screen.tsx`](../apps/showcase/patterns/commerce-social/screens/user-profile-screen.tsx).
- **Excluded application logic:** Profile/post fetching and follow-state persistence.

## Account + Settings

8 screens — source under [`apps/showcase/patterns/account-settings`](../apps/showcase/patterns/account-settings).

### Account

- **Purpose:** Account hub with linked accounts, devices, sign-out, and delete.
- **State contract:** `AccountScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `ListGroup`, `Separator`, `SettingsItem`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onDeleteAccount`, `onLinkedAccountsPress`, `onManageDevicesPress`, `onSignOut`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/account-settings/screens/account-screen.tsx`](../apps/showcase/patterns/account-settings/screens/account-screen.tsx).
- **Excluded application logic:** Account data fetching and destructive account mutations.

### Appearance

- **Purpose:** Theme/appearance preference selection.
- **State contract:** `AppearanceScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Box`, `HStack`, `SegmentedControl`, `SegmentedControlItem`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onThemeChange`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/account-settings/screens/appearance-screen.tsx`](../apps/showcase/patterns/account-settings/screens/appearance-screen.tsx).
- **Excluded application logic:** Theme persistence and system-theme integration.

### Change Password

- **Purpose:** Change password with current/new/confirm fields.
- **State contract:** `ChangePasswordScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `AlertBanner`, `Box`, `Button`, `Field`, `HStack`, `PasswordInput`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onConfirmPasswordChange`, `onCurrentPasswordChange`, `onNewPasswordChange`, `onSubmit`, `onSuccess`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/account-settings/screens/change-password-screen.tsx`](../apps/showcase/patterns/account-settings/screens/change-password-screen.tsx).
- **Excluded application logic:** Password verification and the change mutation.

### Edit Profile

- **Purpose:** Edit profile fields and avatar with save.
- **State contract:** `EditProfileScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `AlertBanner`, `Avatar`, `Box`, `Button`, `Field`, `HStack`, `Input`, `Textarea`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onBioChange`, `onChangeAvatar`, `onDisplayNameChange`, `onEmailChange`, `onSave`, `onUsernameChange`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/account-settings/screens/edit-profile-screen.tsx`](../apps/showcase/patterns/account-settings/screens/edit-profile-screen.tsx).
- **Excluded application logic:** Avatar upload, validation, and profile persistence.

### Notification Settings

- **Purpose:** Toggle notification channels and categories.
- **State contract:** `NotificationSettingsScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `ListGroup`, `Separator`.
- **Ownership boundary:** the screen renders UI and raises intent through `onActivityChange`, `onEmailChange`, `onEnabledChange`, `onMarketingChange`, `onPushChange`, `onRemindersChange`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/account-settings/screens/notification-settings-screen.tsx`](../apps/showcase/patterns/account-settings/screens/notification-settings-screen.tsx).
- **Excluded application logic:** Preference persistence and push-registration.

### Privacy Security

- **Purpose:** Privacy and security settings hub with sessions and 2FA entries.
- **State contract:** `PrivacySecurityScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Badge`, `ListGroup`, `Separator`, `SettingsItem`.
- **Ownership boundary:** the screen renders UI and raises intent through `onBlockedUsersPress`, `onChangePasswordPress`, `onDiscoverableChange`, `onExportDataPress`, `onManageDevicesPress`, `onProfileVisibilityPress`, `onRevokeOtherSessionsPress`, `onTwoFactorPress`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/account-settings/screens/privacy-security-screen.tsx`](../apps/showcase/patterns/account-settings/screens/privacy-security-screen.tsx).
- **Excluded application logic:** Session management, 2FA setup, and data-export execution.

### Profile

- **Purpose:** Personal profile summary routing to edit, account, and activity.
- **State contract:** `ProfileScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Box`, `HStack`, `ListGroup`, `Separator`, `SettingsItem`, `Text`, `VStack`.
- **Ownership boundary:** the screen renders UI and raises intent through `onEditProfile`, `onOpenAccount`, `onOpenActivity`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/account-settings/screens/profile-screen.tsx`](../apps/showcase/patterns/account-settings/screens/profile-screen.tsx).
- **Excluded application logic:** Profile fetching and navigation.

### Settings

- **Purpose:** Top-level settings menu routing to account, privacy, and appearance.
- **State contract:** `SettingsScreenProps` (fully controlled — state and data are caller-owned props).
- **Composition:** `Badge`, `ListGroup`, `ListGroupHeader`, `Separator`, `SettingsItem`, `Switch`.
- **Ownership boundary:** the screen renders UI and raises intent through `onAboutPress`, `onAccountPress`, `onAppearancePress`, `onNotificationsEnabledChange`, `onNotificationsPress`, `onPrivacyPress`, `onSupportPress`; the caller owns the state and side effects behind them.
- **Responsive:** follows the [responsive layout contract](responsive-layout.md) (bounded content width, safe-area-aware shells).
- **Accessibility:** inherits the per-component [accessibility contract](accessibility-contract.md); no bespoke a11y overrides.
- **Related components:** see the [component reference](component-reference.md) entries for the composition above.
- **Source ownership:** copy the screen and its BeeUI parts into your app via `pnpm beeui add <component>`; the screen source is [`patterns/account-settings/screens/settings-screen.tsx`](../apps/showcase/patterns/account-settings/screens/settings-screen.tsx).
- **Excluded application logic:** Preference persistence and navigation.
